import ComingSoon from "@/components/dashboard/ComingSoon";

export default function TailoredPage() {
  return (
    <ComingSoon
      eyebrow="версии под вакансии"
      title="Адаптации"
      description="Здесь будут версии резюме, заточенные под конкретные вакансии, с match score и историей правок."
    />
  );
}
